# SSH MCP template

This service intentionally provides no remote-execution capability. Before adding
one, define a bank-approved authentication method, target allowlist, command
policy, session recording, audit sink, host-key verification, and break-glass
procedure. Keep it on the `ssh` Compose profile until that review is complete.
