name: todo-tool
prompt: |
  请用 todo_write 工具建立一条任务列表，包含两项：第一项是「安装插件」，第二项是「跑评测」。
  必须真实调用 todo_write 工具；完成后只回复一个词：TODODONE。
require_plugins: ["@deepseek-ai/dsh-tool-todo"]
assert:
  turn_end: completed
  tools_called: ["todo_write"]
  output_contains: ["TODODONE"]
  max_steps: 8
  max_tokens: 30000
